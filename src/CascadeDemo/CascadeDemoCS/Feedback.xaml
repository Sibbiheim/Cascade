<Page
    x:Class="CascadeDemoCS.Feedback"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CascadeDemoCS"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer HorizontalScrollMode="Disabled">
        <Border Style="{StaticResource ScenarioContainerStyle}">
            <StackPanel>
                <StackPanel Margin="0,30,0,29">
                    <TextBlock Text="Feedback" Style="{StaticResource HeaderTextBlockStyle}" />
                </StackPanel>

                <!-- Scenario 1 -->
                <StackPanel Style="{StaticResource SettingsFlyoutSectionStyle}">
                    <TextBlock Text="File bugs and make suggestions on Github" Margin="0,0,0,10" Style="{StaticResource SubheaderTextBlockStyle}" />
                </StackPanel>

                <!-- Scenario 2 -->
                <StackPanel Style="{StaticResource SettingsFlyoutSectionStyle}">
                    <TextBlock Text="Send us an email" Margin="0,0,0,10" Style="{StaticResource SubheaderTextBlockStyle}" />
                </StackPanel>
            </StackPanel>
        </Border>
    </ScrollViewer>
</Page>
